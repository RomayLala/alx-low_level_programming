#include "main.h"

/**
 * set_string - Sets the value of a pointer to a char.
 * @s: Pointer to a pointer to a char (string to be set).
 * @to: Pointer to a char (string to set s to).
 *
 * Return: void
 */
void set_string(char **s, char *to)
{
	*s = to;
}
