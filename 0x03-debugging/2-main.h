#ifndef MAIN_H
#define MAIN_H

/* Standard library for size_t definition */
#include <stddef.h>

/* _putchar - writes the character c to stdout */
int _putchar(char c);

/* largest_number - returns the largest of 3 numbers */
int largest_number(int a, int b, int c);

#endif /* MAIN_H */
